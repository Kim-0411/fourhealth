<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.fourhealth.mapper.MemberMapper">
 	<insert id="addSns" parameterType="SnsUserDto">
 		INSERT INTO tb_member
		 (user_sns_code, user_id, user_sns_contents, user_upload_file, user_sns_reg_date)
		VALUES (
		  #{userSnsCode}
		 ,#{userId}
		 ,#{userSnsContents}
		 ,#{userSnsUploadFile}
		 , NOW());
 	</insert>
  </mapper>