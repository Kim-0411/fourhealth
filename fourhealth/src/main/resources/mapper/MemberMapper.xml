<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fourhealth.mapper.MemberMapper">
	
	<!--트레이너가 자기 본인의 프로모션에 등록된 회원중 한명의 회원의 정보를 가져올때.  -->
	<select id="getTrainerMyMatchingUserInfo" parameterType="String" resultType="UserDto">
		SELECT
			u.user_id AS 'userId',
			u.user_first_height AS 'userFirstHeight',
			u.user_first_weight AS 'userFirstWeight',
			u.user_first_goal AS 'userFirstGoal'

		FROM
			tb_matching_user_trainer AS mut
		INNER JOIN
			tb_user AS u
		ON
			mut.user_id = #{userId};
	</select>
	


</mapper>